<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.stameni.com.quizforall">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".common.base.BaseApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".ui.forgotPassword.passwordReset.PasswordResetActivity">
        </activity>
        <activity
                android:name="com.stameni.com.quizforall.ui.login.mainActivity.MainScreenActivity"
                android:hardwareAccelerated="true">
        </activity>
        <activity android:name=".ui.forgotPassword.ForgotPasswordActivity">
        </activity>
        <activity android:name=".ui.signUp.SignupActivity">
        </activity>
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>